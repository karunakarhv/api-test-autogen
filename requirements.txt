# Core
pyyaml>=6.0
requests>=2.31

# Testing
pytest>=7.4
pytest-html>=4.0
pytest-json-report>=1.5

# Optional — LLM enrichment
anthropic>=0.25

# schemathesis
schemathesis

# Optional — Postman runner (install separately via npm)
# npm install -g newman
# npm install -g newman-reporter-htmlextra
