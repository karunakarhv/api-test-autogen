"""
test_user.py
Auto-generated by generate_pytest.py
Source spec : petstore.yaml
Tag         : user
Tests       : 6

DO NOT EDIT MANUALLY — re-run generate_pytest.py to regenerate.
"""
import requests
import pytest

def test_post_user__happy_path(base_url, auth_headers):
        """
        [USER] Create user
        Scenario : happy_path
        Expected : HTTP 200
        """
        url = f"{base_url}/user"
        payload = {
        "id": 10,
        "username": "test_value",
        "firstName": "test_value",
        "lastName": "test_value",
        "email": "test@example.com",
        "password": "test_value",
        "phone": "test_value",
        "userStatus": 5,
    }
        response = requests.post(
            url,
            headers=auth_headers,
            json=payload
        )
    
        assert response.status_code == 200, (
            f"Expected 200, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )
    
        # Basic schema validation
        if response.status_code < 300:
            data = response.json()
            assert isinstance(data, (dict, list)), \
                "Response should be a JSON object or array"

def test_get_user_login__happy_path(base_url, auth_headers):
        """
        [USER] Logs user into the system
        Scenario : happy_path
        Expected : HTTP 200
        """
        url = f"{base_url}/user/login"
        params  = {
        "username": "test_value",
        "password": "test_value",
    }
        response = requests.get(
            url,
            headers=auth_headers,
            params=params
        )
    
        assert response.status_code == 200, (
            f"Expected 200, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_get_user_logout__happy_path(base_url, auth_headers):
        """
        [USER] Logs out current logged in user session
        Scenario : happy_path
        Expected : HTTP 200
        """
        url = f"{base_url}/user/logout"
        response = requests.get(
            url,
            headers=auth_headers
        )
    
        assert response.status_code == 200, (
            f"Expected 200, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_get_user_username__happy_path(base_url, auth_headers):
        """
        [USER] Get user by username
        Scenario : happy_path
        Expected : HTTP 200
        """
        url = f"{base_url}/user/test_value"
        response = requests.get(
            url,
            headers=auth_headers
        )
    
        assert response.status_code == 200, (
            f"Expected 200, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_put_user_username__happy_path(base_url, auth_headers):
        """
        [USER] Update user
        Scenario : happy_path
        Expected : HTTP 200
        """
        url = f"{base_url}/user/test_value"
        payload = {
        "id": 10,
        "username": "test_value",
        "firstName": "test_value",
        "lastName": "test_value",
        "email": "test@example.com",
        "password": "test_value",
        "phone": "test_value",
        "userStatus": 5,
    }
        response = requests.put(
            url,
            headers=auth_headers,
            json=payload
        )
    
        assert response.status_code == 200, (
            f"Expected 200, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )
    
        # Basic schema validation
        if response.status_code < 300:
            data = response.json()
            assert isinstance(data, (dict, list)), \
                "Response should be a JSON object or array"

def test_delete_user_username__happy_path(base_url, auth_headers):
        """
        [USER] Delete user
        Scenario : happy_path
        Expected : HTTP 200
        """
        url = f"{base_url}/user/test_value"
        response = requests.delete(
            url,
            headers=auth_headers
        )
    
        assert response.status_code == 200, (
            f"Expected 200, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )
