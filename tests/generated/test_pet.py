"""
test_pet.py
Auto-generated by generate_pytest.py
Source spec : petstore.yaml
Tag         : pet
Tests       : 24

DO NOT EDIT MANUALLY — re-run generate_pytest.py to regenerate.
"""
import requests
import pytest

def test_post_pet__happy_path(base_url, auth_headers):
        """
        [PET] Add a new pet
        Scenario : happy_path
        Expected : HTTP 200
        """
        url = f"{base_url}/pet"
        payload = {
        "id": 10,
        "name": "test_value",
        "category": "test_value",
        "photoUrls": ["test_value"],
        "tags": ["test_value"],
        "status": "available",
    }
        response = requests.post(
            url,
            headers=auth_headers,
            json=payload
        )
    
        assert response.status_code == 200, (
            f"Expected 200, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )
    
        # Basic schema validation
        if response.status_code < 300:
            data = response.json()
            assert isinstance(data, (dict, list)), \
                "Response should be a JSON object or array"

def test_post_pet__missing_required(base_url, auth_headers):
        """
        [PET] Missing required field: name
        Scenario : missing_required
        Expected : HTTP 422
        """
        url = f"{base_url}/pet"
        payload = {
        "id": 10,
        "name": "test_value",
        "category": "test_value",
        "photoUrls": ["test_value"],
        "tags": ["test_value"],
        "status": "available",
    }
        response = requests.post(
            url,
            headers=auth_headers,
            json=payload
        )
    
        assert response.status_code == 422, (
            f"Expected 422, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_post_pet__missing_required(base_url, auth_headers):
        """
        [PET] Missing required field: photoUrls
        Scenario : missing_required
        Expected : HTTP 422
        """
        url = f"{base_url}/pet"
        payload = {
        "id": 10,
        "name": "test_value",
        "category": "test_value",
        "photoUrls": ["test_value"],
        "tags": ["test_value"],
        "status": "available",
    }
        response = requests.post(
            url,
            headers=auth_headers,
            json=payload
        )
    
        assert response.status_code == 422, (
            f"Expected 422, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_post_pet__invalid_enum(base_url, auth_headers):
        """
        [PET] Invalid enum in body field: status
        Scenario : invalid_enum
        Expected : HTTP 400
        """
        url = f"{base_url}/pet"
        payload = {
        "id": 10,
        "name": "test_value",
        "category": "test_value",
        "photoUrls": ["test_value"],
        "tags": ["test_value"],
        "status": "available",
    }
        response = requests.post(
            url,
            headers=auth_headers,
            json=payload
        )
    
        assert response.status_code == 400, (
            f"Expected 400, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_post_pet__no_auth(base_url, no_auth_headers):
        """
        [PET] Request without authentication
        Scenario : no_auth
        Expected : HTTP 401
        """
        url = f"{base_url}/pet"
        payload = {
        "id": 10,
        "name": "test_value",
        "category": "test_value",
        "photoUrls": ["test_value"],
        "tags": ["test_value"],
        "status": "available",
    }
        response = requests.post(
            url,
            headers=no_auth_headers,
            json=payload
        )
    
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_put_pet__happy_path(base_url, auth_headers):
        """
        [PET] Update an existing pet
        Scenario : happy_path
        Expected : HTTP 200
        """
        url = f"{base_url}/pet"
        payload = {
        "id": 10,
        "name": "test_value",
        "category": "test_value",
        "photoUrls": ["test_value"],
        "tags": ["test_value"],
        "status": "available",
    }
        response = requests.put(
            url,
            headers=auth_headers,
            json=payload
        )
    
        assert response.status_code == 200, (
            f"Expected 200, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )
    
        # Basic schema validation
        if response.status_code < 300:
            data = response.json()
            assert isinstance(data, (dict, list)), \
                "Response should be a JSON object or array"

def test_put_pet__missing_required(base_url, auth_headers):
        """
        [PET] Missing required field: name
        Scenario : missing_required
        Expected : HTTP 422
        """
        url = f"{base_url}/pet"
        payload = {
        "id": 10,
        "name": "test_value",
        "category": "test_value",
        "photoUrls": ["test_value"],
        "tags": ["test_value"],
        "status": "available",
    }
        response = requests.put(
            url,
            headers=auth_headers,
            json=payload
        )
    
        assert response.status_code == 422, (
            f"Expected 422, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_put_pet__missing_required(base_url, auth_headers):
        """
        [PET] Missing required field: photoUrls
        Scenario : missing_required
        Expected : HTTP 422
        """
        url = f"{base_url}/pet"
        payload = {
        "id": 10,
        "name": "test_value",
        "category": "test_value",
        "photoUrls": ["test_value"],
        "tags": ["test_value"],
        "status": "available",
    }
        response = requests.put(
            url,
            headers=auth_headers,
            json=payload
        )
    
        assert response.status_code == 422, (
            f"Expected 422, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_put_pet__invalid_enum(base_url, auth_headers):
        """
        [PET] Invalid enum in body field: status
        Scenario : invalid_enum
        Expected : HTTP 400
        """
        url = f"{base_url}/pet"
        payload = {
        "id": 10,
        "name": "test_value",
        "category": "test_value",
        "photoUrls": ["test_value"],
        "tags": ["test_value"],
        "status": "available",
    }
        response = requests.put(
            url,
            headers=auth_headers,
            json=payload
        )
    
        assert response.status_code == 400, (
            f"Expected 400, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_put_pet__no_auth(base_url, no_auth_headers):
        """
        [PET] Request without authentication
        Scenario : no_auth
        Expected : HTTP 401
        """
        url = f"{base_url}/pet"
        payload = {
        "id": 10,
        "name": "test_value",
        "category": "test_value",
        "photoUrls": ["test_value"],
        "tags": ["test_value"],
        "status": "available",
    }
        response = requests.put(
            url,
            headers=no_auth_headers,
            json=payload
        )
    
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_get_pet_findByStatus__happy_path(base_url, auth_headers):
        """
        [PET] Finds Pets by status
        Scenario : happy_path
        Expected : HTTP 200
        """
        url = f"{base_url}/pet/findByStatus"
        params  = {
        "status": "available",
    }
        response = requests.get(
            url,
            headers=auth_headers,
            params=params
        )
    
        assert response.status_code == 200, (
            f"Expected 200, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_get_pet_findByStatus__invalid_enum(base_url, auth_headers):
        """
        [PET] Invalid enum for param: status
        Scenario : invalid_enum
        Expected : HTTP 400
        """
        url = f"{base_url}/pet/findByStatus"
        params  = {
        "status": "__invalid_enum_value__",
    }
        response = requests.get(
            url,
            headers=auth_headers,
            params=params
        )
    
        assert response.status_code == 400, (
            f"Expected 400, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_get_pet_findByStatus__no_auth(base_url, no_auth_headers):
        """
        [PET] Request without authentication
        Scenario : no_auth
        Expected : HTTP 401
        """
        url = f"{base_url}/pet/findByStatus"
        params  = {
        "status": "available",
    }
        response = requests.get(
            url,
            headers=no_auth_headers,
            params=params
        )
    
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_get_pet_findByTags__happy_path(base_url, auth_headers):
        """
        [PET] Finds Pets by tags
        Scenario : happy_path
        Expected : HTTP 200
        """
        url = f"{base_url}/pet/findByTags"
        params  = {
        "tags": "test_value",
    }
        response = requests.get(
            url,
            headers=auth_headers,
            params=params
        )
    
        assert response.status_code == 200, (
            f"Expected 200, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_get_pet_findByTags__no_auth(base_url, no_auth_headers):
        """
        [PET] Request without authentication
        Scenario : no_auth
        Expected : HTTP 401
        """
        url = f"{base_url}/pet/findByTags"
        params  = {
        "tags": "test_value",
    }
        response = requests.get(
            url,
            headers=no_auth_headers,
            params=params
        )
    
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_get_pet_petId__happy_path(base_url, auth_headers):
        """
        [PET] Find pet by ID
        Scenario : happy_path
        Expected : HTTP 200
        """
        url = f"{base_url}/pet/10"
        response = requests.get(
            url,
            headers=auth_headers
        )
    
        assert response.status_code == 200, (
            f"Expected 200, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_get_pet_petId__wrong_type(base_url, auth_headers):
        """
        [PET] Wrong type for param: petId
        Scenario : wrong_type
        Expected : HTTP 422
        """
        url = f"{base_url}/pet/10"
        response = requests.get(
            url,
            headers=auth_headers
        )
    
        assert response.status_code == 422, (
            f"Expected 422, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_get_pet_petId__no_auth(base_url, no_auth_headers):
        """
        [PET] Request without authentication
        Scenario : no_auth
        Expected : HTTP 401
        """
        url = f"{base_url}/pet/10"
        response = requests.get(
            url,
            headers=no_auth_headers
        )
    
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_post_pet_petId__happy_path(base_url, auth_headers):
        """
        [PET] Updates a pet in the store with form data
        Scenario : happy_path
        Expected : HTTP 200
        """
        url = f"{base_url}/pet/10"
        params  = {
        "name": "test_value",
        "status": "test_value",
    }
        response = requests.post(
            url,
            headers=auth_headers,
            params=params
        )
    
        assert response.status_code == 200, (
            f"Expected 200, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_post_pet_petId__wrong_type(base_url, auth_headers):
        """
        [PET] Wrong type for param: petId
        Scenario : wrong_type
        Expected : HTTP 422
        """
        url = f"{base_url}/pet/10"
        params  = {
        "name": "test_value",
        "status": "test_value",
    }
        response = requests.post(
            url,
            headers=auth_headers,
            params=params
        )
    
        assert response.status_code == 422, (
            f"Expected 422, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_post_pet_petId__no_auth(base_url, no_auth_headers):
        """
        [PET] Request without authentication
        Scenario : no_auth
        Expected : HTTP 401
        """
        url = f"{base_url}/pet/10"
        params  = {
        "name": "test_value",
        "status": "test_value",
    }
        response = requests.post(
            url,
            headers=no_auth_headers,
            params=params
        )
    
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_delete_pet_petId__happy_path(base_url, auth_headers):
        """
        [PET] Deletes a pet
        Scenario : happy_path
        Expected : HTTP 200
        """
        url = f"{base_url}/pet/10"
        response = requests.delete(
            url,
            headers=auth_headers
        )
    
        assert response.status_code == 200, (
            f"Expected 200, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_delete_pet_petId__wrong_type(base_url, auth_headers):
        """
        [PET] Wrong type for param: petId
        Scenario : wrong_type
        Expected : HTTP 422
        """
        url = f"{base_url}/pet/10"
        response = requests.delete(
            url,
            headers=auth_headers
        )
    
        assert response.status_code == 422, (
            f"Expected 422, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )

def test_delete_pet_petId__no_auth(base_url, no_auth_headers):
        """
        [PET] Request without authentication
        Scenario : no_auth
        Expected : HTTP 401
        """
        url = f"{base_url}/pet/10"
        response = requests.delete(
            url,
            headers=no_auth_headers
        )
    
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}\n"
            f"Body: {response.text[:200]}"
        )
